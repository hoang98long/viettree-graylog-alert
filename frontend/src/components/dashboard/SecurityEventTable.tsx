import type { SecurityEvent } from "../../types/event";
import { EventRow } from "./EventRow";
export function SecurityEventTable({ events, onView }: { events: SecurityEvent[]; onView: (id: string) => void }) { return <div className="overflow-x-auto rounded-xl border border-slate-800 bg-slate-900"><table className="w-full min-w-[760px] text-left"><thead className="bg-slate-950/50 text-xs text-slate-500"><tr><th className="px-4 py-3">TIME</th><th className="px-4 py-3">EVENT</th><th className="px-4 py-3">SOURCE</th><th className="px-4 py-3">SEVERITY</th><th className="px-4 py-3">TELEGRAM</th><th className="px-4 py-3">ACTION</th></tr></thead><tbody>{events.length ? events.map(event => <EventRow key={event.event_id} event={event} onView={onView}/>) : <tr><td colSpan={6} className="px-4 py-10 text-center text-sm text-slate-500">No security events found.</td></tr>}</tbody></table></div>; }
